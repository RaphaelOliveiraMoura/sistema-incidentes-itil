import styled from 'styled-components'

export const Wrapper = styled.div`
  padding: 26px;

  form {
    display: flex;
    flex-direction: column;
  }

  form > * {
    margin-bottom: 16px;
  }

  form > button {
    margin-bottom: 8px;
  }
`
